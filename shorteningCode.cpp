#include<bits/stdc++.h>

using namespace std;

//type names
//typedef long long ll;
typedef long double ld;
typedef vector<int> vi;
typedef pair<int, int> pi;
//define macros which are words substituted before compilation
#define ll long long
#define PB push_back
#define MP make_pair
#define F first
//#define S second

#define S(a) second

#define REP(i, a, b) for(int i =1; i<=b; i++)
#define SQ(a) ((a)*(a))

int main(){
    
    cin.tie(0);    
    ll x = 1e15;
    cout << x << "\n";
    
    vi v;    
    vector<pi> v1;
    v1.PB(MP(1,2));
    cout << v1[0].F << " " << v1[0].S(b) << "\n";

    REP(i, 1, 10){
        cout << SQ(i+i) << "\n";
    }
}
    

