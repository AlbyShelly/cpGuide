#include<bits/stdc++.h>

using namespace std;


int main (){

    ios::sync_with_stdio(0); //disable sync with c streams
    cin.tie(0); //prevents flush on cout before reading
    //newline is better that endl because endl causes a flush operation
   
    //redirect buffers from files to files
    freopen("input.txt", "r", stdin);
    freopen("output.txt", "w", stdout);
}           
